# PChat_MCP_RAG 云部署实践总结（面试版）

> 仓库：https://github.com/Kayn7755/PChat_MCP_RAG  
> 目标环境：单机云服务器（Docker Compose）+ GitHub Actions CI/CD  
> 状态：已上线；`postgres` / `agent` / `rag-dashboard` 均 `healthy`

本文面向面试口述与简历项目描述，可与运维手册 `DEPLOY.md` 对照阅读。

---

## 1. 项目一句话

把 **PChatMind Agent**（对话 / 工具调用）与 **Modular RAG MCP Server**（混合检索知识库）做成 monorepo，用 **Docker Compose** 部署到云主机，并用 **GitHub Actions** 实现「推 `main` 即发布」。

核心架构选择：**Agent 作为 MCP Client，RAG 作为独立 MCP Server（stdio 子进程）**——检索与生成解耦，知识库能力可单独演进。

---

## 2. 系统架构

```
浏览器
  ├─ :8080  Agent UI / FastAPI（对话、工具调用、会话落库）
  └─ :8501  Modular Dashboard（文档入库 / collection 管理）
                │
                ▼
┌──────────────────────────────────────────────────────────┐
│  Docker Compose（同一台云 VM）                              │
│                                                          │
│  pchat-agent                                             │
│    · FastAPI + 前端静态资源                                │
│    · KnowledgeTool → ModularRagMcpClient                 │
│    · 子进程：python -m src.mcp_server.server（stdio MCP） │
│         └─ HybridSearch（Dense + BM25 + RRF + 可选 Rerank）│
│                                                          │
│  pchat-rag-dashboard                                     │
│    · Streamlit 管理台                                    │
│    · 与 Agent 共享 volume: modular_data（Chroma/BM25）    │
│                                                          │
│  pchat-postgres（pgvector 镜像）                          │
│    · Agent 会话 / 智能体配置等业务表（非 MCP 主检索库）      │
└──────────────────────────────────────────────────────────┘
```

### 职责划分（面试常问）

| 能力 | 谁负责 |
|------|--------|
| 对话、工具决策、最终生成 | Agent + LLM API |
| PDF/文档解析、切分、向量化入库 | Modular Dashboard / Ingestion |
| 语义+关键词混合检索、引用 | Modular MCP `query_knowledge_hub` |
| 会话历史、Agent 配置 | PostgreSQL |

**不要说错：** 上线 MCP RAG 后，PDF **不应**再走 Agent 内 Docling→pgvector；云镜像已用精简依赖（`requirements.docker.txt`），去掉 docling/torch，避免把「旧本地 RAG」和「MCP RAG」混为一谈。

---

## 3. 部署形态与产物

| 服务 | 容器名 | 端口 | 作用 |
|------|--------|------|------|
| postgres | pchat-postgres | 内网 5432 | 业务库；`init_db.sql` 初始化 |
| agent | pchat-agent | **8080** | 用户对话入口 |
| rag-dashboard | pchat-rag-dashboard | **8501** | 知识库灌数 |

关键设计：

- **共享 volume `modular_data`**：Dashboard 写入的向量/BM25，Agent 侧 MCP 子进程才能读到。
- **密钥只在服务器 `.env`**：不进 Git；Actions 部署时解压代码会**保留**已有 `.env`。
- 仓库：https://github.com/Kayn7755/PChat_MCP_RAG

---

## 4. 完整部署流程（从 0 到可用）

### 4.1 准备云服务器

1. 开通 Linux 云主机（如阿里云 ECS），安全组放行 **22 / 8080 / 8501**（生产再收敛到 80/443）。
2. 安装 Docker + Compose。
3. 创建目录并准备环境变量：

```bash
sudo mkdir -p /opt/pchat_mcp_rag && sudo chown $USER:$USER /opt/pchat_mcp_rag
cd /opt/pchat_mcp_rag
# 首次需有一份 .env（可从仓库 .env.example 复制后填写）
# POSTGRES_PASSWORD、LLM Key、OPENAI_API_KEY（或 Modular 所用嵌入 Key）等
```

### 4.2 配置 GitHub Actions Secrets

仓库 → Settings → Secrets and variables → Actions：

| Secret | 含义 |
|--------|------|
| `SSH_HOST` | 云主机公网 IP |
| `SSH_USER` | SSH 用户（如 `root`） |
| `SSH_KEY` | 部署私钥（推荐 **Base64 单行**，避免 Windows 粘贴丢换行） |
| `SSH_PORT` | 可选，默认 22 |
| `DEPLOY_DIR` | 可选，默认 `/opt/pchat_mcp_rag` |

本机生成密钥示例：

```bash
ssh-keygen -t ed25519 -C "gha-deploy" -f ~/.ssh/gha_deploy -N ""
# 公钥 → 服务器 ~/.ssh/authorized_keys
# 私钥 Base64 → GitHub Secret SSH_KEY
```

### 4.3 CI/CD 流水线（最终方案）

触发：`push` 到 `main` 或手动 `workflow_dispatch`。

```
GitHub Actions Runner（能访问 GitHub）
  ① checkout 代码
  ② 解码 SSH 私钥
  ③ tar 打包（排除 .git / node_modules / .venv / data）
  ④ scp 上传到服务器 /tmp
  ⑤ ssh 远程：
       · 解压到 /opt/pchat_mcp_rag（备份并恢复 .env）
       · setsid docker compose up -d --build（后台构建）
       · 轮询 http://127.0.0.1:8080/docs 与 :8501/_stcore/health
  ⑥ 健康检查通过 → Deploy OK
```

**为何不用「服务器 git pull」？**  
部分云主机访问 `github.com:443` 超时。改为 **Runner 拉代码 + SCP 下发**，服务器只构建/启动容器。

**为何不用 appleboy/ssh-action 长任务？**  
其默认 `command_timeout≈10m`，慢速 `docker build`/`pip` 易被掐断；最终改为 **Runner 原生 ssh/scp** + `ServerAliveInterval`，超时只受 Job `timeout-minutes: 120` 约束。

### 4.4 上线验收

```bash
cd /opt/pchat_mcp_rag && docker compose ps
```

期望：三个容器均为 `Up (healthy)`。

浏览器：

- Agent：`http://<公网IP>:8080`
- Dashboard：`http://<公网IP>:8501` → 向 `default` collection 灌文档 → 回 Agent 提问验证 `KnowledgeTool` / MCP 检索。

---

## 5. 踩过的坑与对策（面试加分）

| 问题 | 现象 | 对策 |
|------|------|------|
| Secret 名不对 / 为空 | `missing server host` | 仓库级 Secret：`SSH_HOST` / `SSH_USER` / `SSH_KEY` |
| 私钥粘贴损坏 | `ssh: no key found` | Base64 存 Secret，Workflow 里解码 |
| 服务器拉不到 GitHub | `Failed to connect to github.com:443` | SCP 部署，禁止依赖服务器 `git fetch` |
| SSH 10 分钟超时 | `Run Command Timeout` | 弃用 appleboy 长命令；原生 ssh + 后台 compose + 轮询 |
| PyPI/Debian 慢 | pip/apt Read timed out | 镜像内改阿里云 apt、清华 pip、npmmirror |
| Docling 拉 CUDA/torch | 构建数小时 / 超时 | 云镜像用 `requirements.docker.txt`，PDF 入库走 Modular |
| Hatchling 缺 README | `Readme file does not exist` | Dashboard Dockerfile `COPY README.md`，修正 `.dockerignore` |
| Agent/Dashboard 数据不一致 | 入库了但检索空 | 共用 `modular_data` volume；collection 名与 `JCHATMIND_MCP_DEFAULT_COLLECTION` 一致 |

---

## 6. RAG 接入方式（方案 B）

- **Agent**：`McpRagService` + `ModularRagMcpClient`（JSON-RPC over stdio）。
- **工具**：`KnowledgeTool` → `query_knowledge_hub(query, top_k, collection)`。
- **知识库 ID**：默认映射到 MCP `collection`（可用环境变量 JSON 映射表）。
- **生成**：仍由 Host Agent 的 LLM 完成；MCP 侧重「带引用的上下文」。

对比：若做成进程内直接调 `HybridSearch`（方案 A），延迟更低但耦合更高；本项目为隔离与可替换性选择 MCP。

---

## 7. 关键文件索引

| 路径 | 作用 |
|------|------|
| `docker-compose.yml` | 三服务编排与 volume |
| `PChatMind-python-agent/Dockerfile` | Agent 多阶段构建（Node UI + Python） |
| `PChatMind-python-agent/requirements.docker.txt` | 云精简依赖 |
| `MODULAR-RAG-MCP-SERVER-main/Dockerfile` | Dashboard 镜像 |
| `.github/workflows/deploy.yml` | CI/CD |
| `.env.example` | 环境变量模板 |
| `DEPLOY.md` | 运维速查 |
| `jchatmind_agent/mcp_rag_client.py` | MCP stdio Client |
| `jchatmind_agent/rag_service.py` | `McpRagService` |

---

## 8. 简历可写要点（示例）

- 设计并落地 **Agent + Modular RAG MCP** 的 monorepo 云部署：Compose 三服务、共享向量卷、stdio MCP 检索。
- 搭建 **GitHub Actions → SSH/SCP → Docker Compose** 流水线；针对国内云主机访问 GitHub/PyPI 不稳定设计 **离线下发 + 镜像加速**。
- 排查并解决 SSH 超时、私钥 Secret 损坏、CUDA 依赖拖垮构建等问题，将首次镜像构建收敛到可接受时长并实现健康检查验收。

---

## 9. 面试口述稿（约 1～2 分钟）

> 我做了一个把对话 Agent 和 Modular RAG 通过 MCP 打通的项目。Agent 负责工具调用和生成，RAG 以独立 MCP Server 提供混合检索。部署上用 Docker Compose 在一台云主机跑 Postgres、Agent、Dashboard 三个容器，知识库数据用共享 volume，保证入库和检索一致。  
> CI/CD 用 GitHub Actions：Runner 拉代码打包，SCP 到服务器再 `docker compose up --build`，因为云主机经常访问不了 GitHub。过程中遇到过 SSH Action 默认十分钟超时、pip 拉 torch/CUDA 过慢、Secret 粘贴坏私钥等问题，最后改成原生 SSH、精简云依赖、Base64 传密钥，并加上健康检查。现在三个服务都是 healthy，可以通过 8080 对话、8501 灌文档。

---

## 10. 后续可讲的演进（加分）

- 前面加 Nginx/Caddy + HTTPS，收敛公网端口。
- 镜像构建放到 CI（buildx）再推到容器仓库，服务器只 `pull`，进一步缩短发布窗口。
- Agent 上传入口与 Modular ingestion API 打通，避免两套入库心智。
- 可观测性：MCP trace、compose 日志采集、告警。

---

*文档对应一次真实上线实践，运维细节以 `DEPLOY.md` 与仓库当前 Workflow 为准。*
